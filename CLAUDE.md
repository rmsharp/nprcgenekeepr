## SESSION PROTOCOL — FOLLOW BEFORE DOING ANYTHING

**Read and follow `SESSION_RUNNER.md` step by step.** It is your operating procedure for every session. It tells you what to read, when to stop, and how to close out.

**Three rules you will be tempted to violate:**
1. **Orient first** — Read SAFEGUARDS.md → SESSION_NOTES.md → check GitHub Issues (or BACKLOG.md if no repo) → run `methodology_dashboard.py` → git status → report findings → WAIT FOR THE USER TO SPEAK
2. **1 and done** — One deliverable per session. When it's complete, close out. Do not start the next thing.
3. **Auto-close** — When done: evaluate previous handoff, self-assess, document learnings, write handoff notes, commit, report, STOP.

`SESSION_RUNNER.md` documents known failure modes and their countermeasures. The protocol compensates for documented tendencies to skip orientation, skip close-out, and continue past the deliverable.

---

# nprcgenekeepr

## Project Overview

**nprcgenekeepr** (Version 1.0.8) is an R package implementing Genetic Tools for Colony Management. Initially conceived and developed as a Shiny web application at the Oregon National Primate Research Center (ONPRC), it has been enhanced to have more capability as a Shiny application and to expose functions for use either interactively or in R scripts.

This work has been supported in part by NIH grants P51 RR13986 to the Southwest National Primate Research Center and P51 OD011092 to the Oregon National Primate Research Center.

### Core Functions

1. **Quality Control** - Validation of studbooks from text files, Excel workbooks, and LabKey EHR pedigrees. Checks include:
   - Parent record verification
   - Sex validation (no male dams, female sires)
   - Duplicate detection
   - Date validation
   - Minimum parent age verification (default 2 years)

2. **Pedigree Creation** - Building pedigrees from animal lists using LabKey EHR integration via `Rlabkey` package

3. **Age-Sex Pyramid Plots** - Visual display of living animals by age and sex for demographic analysis

4. **Genetic Value Analysis Reports** - Ranking scheme using:
   - Mean kinship (indicates inter-relatedness with colony)
   - Genome uniqueness (indicates presence of rare alleles)
   - Animals with low mean kinship or high genome uniqueness rank higher

5. **Breeding Group Formation** - Creating potential breeding groups that:
   - Avoid mating of closely related animals
   - Support optional sex ratio constraints
   - Support harem group configuration
   - Maximize genetic diversity

### Package Structure

- `R/` - Package functions and Shiny modules
- `inst/application/` - Original monolithic Shiny application (server.R, ui.R)
- `inst/extdata/` - Example data and configuration files
- `tests/testthat/` - Unit tests

### Running the Application

```r
library(nprcgenekeepr)
runGeneKeepR()
```

### Key References

Vinson, A; Raboin, MJ. "A Practical Approach for Designing Breeding Groups to Maximize Genetic Diversity in a Large Colony of Captive Rhesus Macaques (*Macaca mulatta*)" *Journal of the American Association for Laboratory Animal Science*, 2015 Nov, Vol.54(6), pp.700-707

### Online Documentation

https://rmsharp.github.io/nprcgenekeepr/

---

## Development Process Contract

This project uses **Strict Test-Driven Development (TDD)**.
Deviation is a defect.

### TDD Rules:
- Write tests before implementation code
- Each feature branch should include tests
- Ensure both happy paths and all non-happy paths are tested
- Ensure potential edge cases are tested
- Maintain >80% code coverage for new code
- Run full test suite before merging
- Tests should be fast, isolated, and deterministic

### TDD Phases

#### RED
- Write tests only
- Tests must fail
- No implementation code
- No production logic
- No refactoring

#### GREEN
- Write the minimum implementation required to pass tests
- No new functionality
- No refactoring
- No optimization

#### REFACTOR
- Improve structure and readability
- No behavior changes
- All tests must remain passing

### Enforcement Rules

- The assistant MUST declare the current phase at the top of every response.
- The assistant MUST refuse requests that violate the current phase.
- The assistant MUST ask permission before transitioning between phases — via `AskUserQuestion`, per the **Phase-gate format** below.
- Skipping phases is forbidden.
- Writing implementation code during RED is a violation.
- Ensure potential edge cases are tested
- Maintain >80% code coverage for new code
- Run full test suite before merging
- Tests must be fast, isolated, and deterministic

### Phase-gate format

The "ask permission before transitioning" rule above is satisfied with **`AskUserQuestion`** (the structured prompt), **not** a prose question, at **every** phase transition — so the choice and the exact planned actions are explicit and logged. This is a followed project convention, **not** a `settings.json` hook: there is no "phase transition" harness event, and a hook cannot author options describing the specific next-phase actions.

Each gate is **one** `AskUserQuestion` with this shape (the harness auto-appends a free-text "Other"):

- **Header:** `TDD: <FROM>→<TO>` (e.g. `TDD: RED→GREEN`).
- **Option 1 — "Yes, proceed to <TO>":** spell out the *exact* actions the next phase will take — files, the concrete change, how the failing tests / completion criteria get satisfied — then the downstream verification (full suite, lint, the build-equivalent per "Build / Test / Verify", and any E2E/integration).
- **Option 2 — "Hold / <alternative>":** a concrete alternative — pause to review the RED tests / classification first, OR a narrower next-phase scope (e.g. "docs only; leave X as-is").

**Gated transitions:** `PRE-RED→RED`, `RED→GREEN`, `GREEN→REFACTOR`. A pre-RED **scope or approach** decision that is the author's to make (e.g. which functions are in scope) is a *separate* `AskUserQuestion`, posed before declaring RED. The declare-phase-at-top-of-response and refuse-on-violation rules are unchanged.

### Error Handling

If a response violates TDD:
1. The assistant must acknowledge the violation.
2. The assistant must correct itself.
3. The assistant must reissue a compliant response.

This file supersedes general coding instincts.

---

## Build / Test / Verify

The build-equivalent for this R package (relocated here from `SAFEGUARDS.md` during the 2026-05-31 methodology update so the synced `SAFEGUARDS.md` stays byte-identical to canonical; see `SAFEGUARDS.md` "Verify the Build Equivalent"):

| Purpose | Command | Pass criteria |
|---|---|---|
| Full package check | `devtools::check()` or `R CMD check` | No errors, no warnings, no notes (ideally) |
| Test suite | `devtools::test()` or `testthat::test_local()` | All tests pass |

**Fast single-file test:** `Rscript -e 'suppressMessages(pkgload::load_all(".", quiet=TRUE)); testthat::test_file("tests/testthat/test_X.R", reporter="summary")'`

**Clean regression read** (the `test-app-*`/`test-e2e-*` files are pre-existing baseline noise — see Learning #2/#4 below): `as.data.frame(testthat::test_dir("tests/testthat", reporter="silent", stop_on_failure=FALSE))`, then check `sum(failed)` **and** `sum(error)`, isolating true offenders with `!grepl("test-app-|test-e2e-", file)`.

---

## Project-Specific Methodology Adaptations

*Additions and overrides to the base methodology at `SESSION_RUNNER.md` and `SAFEGUARDS.md` (synced from https://github.com/rmsharp/methodology, not project-owned). The base files govern unless explicitly overridden here. **Do not edit the synced files** — put customizations here so `bin/sync` stays friction-free (see BOOTSTRAP "Updating an existing project").*

### Additional Phase 0 steps

(none)

### Additional task-to-workstream mappings

(none — but see the Development Process Contract override below.)

### Development Process Contract override

This project runs **Strict Test-Driven Development** (see the "Development Process Contract" section above). This is a project-specific override of the base methodology's general development guidance: tests are written before implementation, every response declares its TDD phase (RED / GREEN / REFACTOR), and phase transitions require permission. It supersedes general coding instincts but operates *within* the SESSION_RUNNER protocol (orient → one deliverable → close out). Implementation and bug-fix sessions therefore follow the chosen workstream **and** the RED→GREEN→REFACTOR gates.

### Project-specific Learnings

*Migrated verbatim from `SESSION_RUNNER.md`'s Learnings table during the 2026-05-31 methodology update (Session 10) so the synced runner stays byte-identical to canonical. These are nprcgenekeepr-specific institutional memory from Sessions 1–9 (the PED/GV audit-fix campaign). The base, methodology-level learnings remain in `SESSION_RUNNER.md`.*

| # | Learning | Source | When to Apply |
|---|----------|--------|---------------|
| 1 | Plan-mode output is a draft, not a verified plan. When a prompt contains a multi-phase plan with "implement," the deliverable is a plan document with evidence-based inventory, not Phase 1 code. The gap: Phase 1's task mapping had no entry for plan-mode handoffs, so the session defaulted to "implement." Structural fix: new mapping row + FM #19. | FM #19 discovery | When a prompt contains a multi-phase plan with "implement" — recognize this as a planning workstream. |
| 2 | `testthat::test_local()` records test ERRORS in a separate `error` column, distinct from `failed` (expectation failures). A regression tally that sums only `failed` can read "0 fail" while 100+ tests are erroring. This repo has 154 pre-existing errors (every `test-app-*`/`test-e2e-*` file calls the undefined `create_test_app()`; they error, not skip, because `shinytest2`+`chromote` are installed). Always sum `failed` AND `error`, and isolate a change's true delta by file set, not a single counter. | Session 3 (NEW-15 fix) regression verification | Whenever verifying "no regressions" via `test_local`/`test_dir` — count both columns; treat pre-existing erroring files as baseline, not new breakage. |
| 3 | HEAD git worktrees won't run here: the worktree lacks `renv/activate.R`, so its `.Rprofile` aborts and `pkgload::load_all` fails. To establish a pre-change baseline, revert the single file in place or use a structural proof (e.g. `git grep` showing a symbol is undefined at HEAD) instead of a worktree suite run. | Session 3 | When you need a clean-HEAD comparison in this repo — skip the worktree; use in-place revert or a `git grep`-on-HEAD argument. |
| 4 | The Shiny `create_test_app` baseline noise behaves differently by runner: `testthat::test_local()` ERRORS on the `test-app-*`/`test-e2e-*` files (≈154 in `error`), but `testthat::test_dir("tests/testthat")` SKIPS them (≈159 in `skipped`, 0 `error`). Either way they are NOT regressions. Cleanest compute-change regression read: `as.data.frame(test_dir("tests/testthat", reporter="silent", stop_on_failure=FALSE))`, then check `sum(failed)`/`sum(error)` and isolate true offenders via `!grepl("test-app-\|test-e2e-", file)`. | Session 4 (NEW-34) regression verification | When verifying "no regressions" — prefer `test_dir` + offender-isolation for a 0/0/0 signal; don't be alarmed by skip↔error count shifts on the e2e files. |
| 5 | For an audit-flagged "latent" crash, prove reachability through the project's CANONICAL pipeline (here `qcStudbook`), not only a hand-crafted input — and account for transforms that can re-establish the guarded condition. NEW-34 needed an empty `pUnknown`; `removeAutoGenIds` re-NA-s U-prefixed parents, so "resolve all NA parents" was insufficient — you also had to avoid U-prefixed parent ids. A read-only fan-out workflow (caller-trace ‖ empirical-repro ‖ masking-critic) settled REACHABLE-vs-MASKED before any code changed. | Session 4 (NEW-34) | Before "fixing" any latent/masked audit item — reproduce via the real pipeline and have an adversarial agent argue MASKED; only then write the RED test. |
| 6 | Reachability for a "silent NA / silent degradation" item usually splits by MECHANISM, not one yes/no. NEW-40 (`findGeneration`): the dangling-parent path is MASKED by `addParents` in `qcStudbook`, but the CYCLE path is REACHABLE through the *full* pipeline because `checkParentAge` drops NA-birth rows (R/checkParentAge.R:91) and so isn't a cycle guard. Enumerate each trigger separately; fix at the single CHOKE POINT (here `if (anyNA(gen)) warning(...)`) so every mechanism is covered at once, and prefer `warning` over `stop` to keep the return contract (all callers unaffected; provably non-spurious because a valid acyclic self-contained pedigree always places every id). COROLLARY: adding a `warning()` can trip an EXISTING test that exercises the degenerate path — after the fix, re-run the full suite and watch the `warning` column (not just `failed`/`error`); update such a test to *expect* the diagnostic, not suppress it. | Session 5 (NEW-40) | When fixing any silent-NA/degradation audit item — verify reachability per-mechanism through the full pipeline, guard at the choke point with a warning, and scan the suite's `warning` column for tests that now trip on the degenerate input. |
| 7 | This repo's `.lintr` suppresses findings by **hardcoded line number** (`.lintr:17-34` excludes specific lines in ~19 files, e.g. `"R/correctParentSex.R" = 70L`). Any edit that inserts/removes lines **above** a suppressed line — adding a roxygen `@details`, an import, or comments — shifts it, so the exclusion no longer lands on the finding and a **pre-existing, intentionally-suppressed lint resurfaces**, reading as "a lint you introduced." NEW-37: my `@details` shifted `if (reportErrors)` from L70→L75, resurfacing the `unnecessary_nesting_linter` that `70L` had hidden. Fix = bump the exclusion's line number (70L→75L), NOT refactor the suppressed (out-of-scope NEW-36/PED-6 dual-return) structure. Confirm the lint is pre-existing by linting the HEAD copy (`git show HEAD:path > /tmp/x.R; Rscript -e 'lintr::lint("/tmp/x.R")'`) before assuming you caused it — don't theorize, verify (cf. Learning #3). | Session 6 (NEW-37) | After editing any file listed in `.lintr` exclusions, especially when you add/remove lines above the suppressed region — re-lint, verify against HEAD, and update the exclusion's line number rather than "fixing" the resurfaced finding. |
| 8 | TWO lessons from NEW-48 (`calcFEFG`/`calcFE`/`calcFG` partial-parentage guard). **(a) The audit's stated MECHANISM can be wrong — reproduce the ACTUAL failure mode before trusting the description.** The audit called NEW-48 "silent NA corruption" (`d[NA,]` → all-NA row contaminates FE/FG). Empirical repro proved it is a **hard CRASH** (`subscript out of bounds`): R's matrix indexing differs by NA type — `m[NA_integer_/NA_logical_, ]` returns a silent NA row, but `m[NA_character_, ]` ERRORS, and `calcFEFG` coerces ids to character (`toCharacter`), so a lone-NA parent is `NA_character_`. The 3-lens workflow (empirical ‖ static ‖ adversarial-critic) all converged on CRASH-not-corruption. **(b) stop-vs-warning backward-safety depends on the CURRENT failure mode — the INVERSE of Learning #4/#6.** Those preferred `warning` because the baseline was a silent *success* (NA return) that `stop` would convert success→failure. NEW-48's baseline is ALREADY a crash, so a clear `stop()` is the contract-PRESERVING choice (no input that currently succeeds now fails — only the cryptic message improves) while `warning()`+degrade would be the behavior change. Reachability was again MIXED: M2 canonical `qcStudbook`→`reportGV` pipeline (incl. shipped app) MASKED by `addUIds` (qcStudbook.R:180); M1 direct exported call + M3 `trimPedigree(removeUninformative=TRUE, addBackParents=FALSE)` via `removeUninformativeFounders.R:52-53` REACHABLE. | Session 7 (NEW-48) | When fixing any audit "silent corruption" item: (1) empirically reproduce the ACTUAL failure mode first — character vs logical/integer NA indexing differ; (2) choose stop-vs-warning by the CURRENT behavior — preserve whatever success/failure contract already exists, improving only the diagnostic. |
| 9 | TWO lessons from NEW-25 (`getProportionLow` empty-input crash). **(a) The audit's prescribed FIX can be as wrong as its stated MECHANISM — empirically confirm BOTH before the RED test (extends Learning #8a).** The audit framed it as "three-way if/else-if has no terminal `else`, so `if (NaN > 0.5)` raises…", which reads as "add a terminal `else`." But `NaN > 0.5` is `NA`, and `if (NA)` errors *"missing value where TRUE/FALSE needed"* at the **FIRST** `if` — before any fall-through — so a terminal `else` would NOT have fixed it. A ~1-min empirical repro caught the non-fix. **(b) Reachability is NOT always a `qcStudbook`-masking question — some audit items target ORPHANED code with ZERO live callers.** `getProportionLow` is `@noRd`; an exhaustive caller-trace (grep all file types for the symbol + `do.call`/`get`/`match.fun`/`eval` + commented-out consumers) found its ONLY consumer is the dead/commented `makeGeneticDiversityDashboard` (NEW-20). So "verify reachability" meant a caller-trace, NOT a masker analysis; the fix is robustness-hardening of a retained helper, and the empty/edge-input contract becomes a pure DOMAIN decision for the author (no live caller constrains it — surface options, don't guess). COROLLARY: when grep on a unique-named symbol settles reachability, that IS "already verified" — do NOT spin up the heavier 3-lens reachability workflow; it would be ceremony (honest reading of ultracode's "unless already verified"). | Session 8 (NEW-25) | When fixing any audit edge/error item: (1) empirically reproduce AND confirm the prescribed remedy actually fixes the reproduced failure — `NaN`/`NA` comparisons yield `NA` and crash at the `if`, not a fall-through; (2) if the symbol has zero live callers (orphaned, often by adjacent dead code), treat reachability as an exhaustive caller-trace and the edge-input behavior as an author decision — and don't over-orchestrate what grep already proves. |
| 10 | TWO lessons from NEW-52 (`cumulateSimKinships` sd undefined for n<2). **(a) An audit's claimed mechanism can be not merely WRONG but UNREACHABLE for the function's VALUE DOMAIN — and the disciplined response under strict TDD is to DISPROVE and NOT fix it (extends #8a/#9a).** NEW-52 had two claimed mechanisms: M1 (`n=1` → `0/0 = NaN`) reproduced, but M2 ("near-constant cells → tiny-negative under the root → `sqrt` NaN") does NOT — `kinship()` returns **dyadic rationals** (sums of powers of ½), *exact* in IEEE-754, so constant-across-sims cells cancel to **exactly 0** (empirical probe: 0 negative numerators, 117 exactly-0, 0 NaN at n=10/100), and the unstable expression is only reachable *through* `cumulateSimKinships`, so non-dyadic data can't reach it. No failing test can be written → no code; a `pmax(num,0)` clamp would be speculative, untestable defensive code. **(b) The stop-vs-warning rule (#8b) can apply BOTH WAYS in ONE function.** `n=1` currently *succeeds silently* (returns valid mean/min/max + NaN sd) → graceful **NA+warning** preserves the success (the NEW-40 side); `n=0` *already crashes* (uninitialized `minKinship`) → clear **stop** preserves the crash-contract (the NEW-48 side). Same function, opposite baselines, opposite contract-preserving remedies. COROLLARY (testing): `is.na(NaN)` is **TRUE** in R — to separate the NA fix from the NaN bug a RED test MUST assert `expect_false(any(is.nan(x)))`, not just `all(is.na(x))` (which passes on both). Reachability was again grep-settled: `cumulateSimKinships` is `@export`ed but a **public orphan** (zero live callers), so no workflow (Learning #9 corollary). | Session 9 (NEW-52) | When fixing any audit numerical/instability item: (1) reproduce EACH claimed mechanism; if one is unreachable for the value domain (exact dyadic/integer inputs), DISPROVE and skip it rather than add untestable defensive code; (2) pick stop-vs-warning per-mechanism by each one's CURRENT baseline (silent-success → warning/NA; already-crash → stop), even within one function; (3) in tests use `is.nan`, not just `is.na`, to distinguish NA from NaN. |
| 11 | **Updating the embedded methodology.** The synced trio (`SESSION_RUNNER.md`, `SAFEGUARDS.md`, `methodology_dashboard.py`) must stay **byte-identical to canonical** (`rmsharp/methodology` `starter-kit/`); `bin/sync`/`bin/status` enforce this and REFUSE to overwrite a `locally modified` file without `--force`. **Project customizations (Learnings, build-equivalent, task mappings, extra Phase 0 steps) live in THIS CLAUDE.md "Project-Specific Methodology Adaptations" section — never in the synced files.** Procedure: clone canonical → diff to separate real customization from staleness → extract customizations into CLAUDE.md → `bin/sync . --source=local --force` → confirm `bin/status` = `current`. Framework docs under `docs/methodology/` are do-not-edit reference copies (refresh by overwrite). The dashboard scores **`CHANGELOG.md`** (not the R-convention `NEWS.md`) as the changelog (+health); `NEWS.md` remains the CRAN-facing release notes. The methodology was last synced to `f32d780` (Session 10, 2026-05-31). | Session 10 (methodology update) | Whenever updating the methodology or editing a synced file: relocate customizations to CLAUDE.md, keep synced files canonical, and re-run `bin/status` to confirm `current`. Don't answer methodology-governed questions from general convention — consult the project's own source of truth (the synced files + `methodology_dashboard.py`). |
| 12 | TWO lessons from NEW-53 (`makeSimPed`/`getPotentialParents`/`createSimKinships` mutate the caller's `ped` via `setDT` without `copy()`). **(a) A NEW bug CLASS — "side-effect-on-the-caller" — where the audit over-claims the HARM, not just the mechanism (extends #8a/#9a/#10a).** The audit said `makeSimPed` "overwrites sire/dam" and `createSimKinships` "adds a population column in place." Empirically BOTH are FALSE: the ONLY leak is a by-reference **class flip** `data.frame`→`data.table` (from `setDT`, which mutates attributes in place). Content is preserved because the post-`setDT` `ped$col[…] <- v` triggers `$<-.data.table`'s shallow-copy-and-rebind, decoupling the local `ped` before any column write. So the realized harm is NOT numerical corruption but **`[`-semantics divergence in the *caller's* later code** (`df[1]` = column, `dt[1]` = ROW; `df[,"x"]` = vector, `dt[,"x"]` = 1-col table). **(b) Reachability for a side-effect bug is a CALL-GRAPH-propagation question, not a `qcStudbook`-masker (NEW-37/40/48) nor a value-domain (NEW-52) question.** Probe the graph: internal callers can INSULATE the leak — `cumulateSimKinships` copies via its line-52 `ped$population <- …` BEFORE the sim loop, so it does NOT leak to its caller and its Monte-Carlo result is correct; the realized exposure is **direct calls to the three `@export`ed functions**. COROLLARIES: (i) the fix is `setDT(ped)` → `ped <- data.table::as.data.table(ped)` — `as.data.table` COPIES for both data.frame and data.table inputs (probed), output byte-identical, and `$<-` never leaks a *new* column to the caller either, so the minimal swap suffices (no unconditional `copy()`). (ii) grep the FULL reference-mutation inventory (`setDT`/`setDF`/`:=`/`set(`/`setattr`/`setkey`…) — the handoff named 2 sites, the audit named makeSimPed+createSimKinships, the real cluster was 3; fix-all-instances (workstream Q4). (iii) RED-test a side-effect bug by asserting the caller's object is UNCHANGED post-call (`class(ped)` stays `"data.frame"`); snapshot content via an **immutable** `paste0(…)` string, NOT `before <- ped` (which shares the SEXP — `setDT`/`setattr` mutate it in place, so a naive before/after `identical` can pass even on buggy code). Mechanism + reachability were deterministic-probe-and-grep-settled → **no workflow** (3rd consecutive session; honest "already verified"). | Session 11 (NEW-53) | When fixing any audit "mutates/corrupts in place" item: (1) empirically separate the REAL leak (often just a class/attr flip by `setDT`) from the audit's claimed data corruption — data.table `$<-` shallow-copies, so content usually survives; (2) assess harm as caller-side `[`-semantics divergence and trace whether internal callers insulate it; (3) fix with `as.data.table` (copies) not `setDT` (by-ref); (4) RED-assert caller-object-unchanged with an immutable snapshot. |
| 13 | THREE lessons from NEW-45 (`geneDrop` period-in-id corruption → enforce "no period in IDs"). **(a) When the audit's mechanism is CORRECT, the live decision often shifts to a DOMAIN-DEFINITION question — does the domain permit the input AT ALL? (the inverse of #8a/#9a/#10a, where the mechanism was wrong).** NEW-45's mechanism was right (a `.` truncates the `strsplit(rownames, ".")` id/parent rebuild). The real fork was "support periods (fix parsing) vs reject periods (enforce the spec)" — and that is settled by the project's OWN documented domain spec, **not** general convention or your preference. Here `inst/extdata/ui_guidance/input_format.html:86-97` documents `id`/`sire`/`dam` as **"Alphanumeric characters (no symbols)"** (live in the app via `R/modInput.R:152-153`; named as authority by `vignettes/manual_components/_input.Rmd:39-40`) → PRECLUDED → **reject, not support**. A read-only **multi-modal domain sweep** (searchers over validation-code ‖ docs ‖ format-conventions ‖ example-data + an adversarial synthesizer that steelmans both sides) is the right tool when the answer could live anywhere — and VERIFY the load-bearing evidence firsthand (Learning #20). Watch for the synthesis caveat: a documented-but-UNENFORCED rule means enforcing it is a real behavior change requiring author sign-off. **(b) Enforcement the user can't SEE is incomplete — Phase 3E.** A new `errorLst` field is a **4-touch** change, not 1: add to `getEmptyErrorLst()` + the `checkErrorLst()` count + **BOTH** hardcoded error renderers (`processQcStudbookResult.R` for the Shiny table, `summary.nprcgenekeeprErr.R` for the summary text — neither has a generic loop) — and it trips structure-count tests (`test_getFocalAnimalPed` field-count 9→10; Learning #6 corollary). A pre-existing test passing on SILENTLY-corrupted output is a tell the bug is real (`test_modGeneticValue`'s `C.003` fixture). **(c)** prefer the **targeted** rule with concrete consequences (`.`-only) over the broad documented spec (full "no symbols") unless the user opts in — full-alphanumeric enforcement would reject legitimate `-`/`_` ids (a bigger behavior change, its own decision). COROLLARY: in a lint-dirty file (e.g. `processQcStudbookResult.R`, 13 pre-existing lints, issue #30) use the codebase-idiomatic `toString()` not `paste(.., collapse=)` so your edit adds **0** net lints. | Session 13 (NEW-45) | When the audit's mechanism checks out: (1) ask whether the fix should SUPPORT or REJECT the input, and settle it from the project's documented domain spec (consult `input_format.html`/vignettes, not convention) — a domain-sweep workflow + firsthand verification; (2) if enforcing a new QC rule, trace the new error field through `getEmptyErrorLst`/`checkErrorLst` AND both hardcoded renderers (Phase 3E), and expect a structure-count test to trip; (3) enforce the targeted rule, not the broad spec, unless the author opts in. |
| 14 | THREE lessons from NEW-46 (`geneDrop` duplicate-id handling → reject with a clear `stop()`). **(a) The audit's "silent wrong values" was a hard CRASH again — the NEW-48 pattern (3rd time after NEW-48/NEW-25) — and the crash SITE also differed from the audit's.** The audit (`geneDrop.R:82-104`) said "parent lookup by rowname; duplicate ids → wrong values," but `geneDrop` crashes at `rownames(ped) <- ids` (`R/geneDrop.R:97`) with base-R's `"duplicate 'row.names' are not allowed"` — *before* the lookup (L118-119) and list-keying (L117) the audit fingered (those are preempted/unreachable). **R's `.rowNamesDF<-` REJECTS duplicate data.frame row.names** (a precursor warning `"non-unique value when setting 'row.names'"`, then a hard error) — it does **not** silently mangle, so any `rownames(x) <- <non-unique vector>` idiom CRASHES. **METHOD TRAP:** a `tryCatch(expr, warning=, error=)` short-circuits on the precursor warning and HIDES whether an error follows (probe v1 read "warning"; probe v2 suppressing/letting-pass the warning revealed the terminal hard error). Always probe PAST a precursor warning to see the true terminal behavior. **(b) Reachability can be DOUBLY-masked by two independent upstream guards, and a fix-all grep can NARROW scope, not just widen it.** The cryptic crash is reachable ONLY via a direct `geneDrop()` call: the canonical `qcStudbook → reportGV → geneDrop` path is masked TWICE — `removeDuplicates` (`qcStudbook.R:277`) AND `kinship`'s own `stop("All id values must be unique")`, which `reportGV` calls (`reportGV.R:81`) BEFORE `geneDrop` (`reportGV.R:92`). The `rownames(x) <- …` idiom inventory found a sibling site `reportGV.R:122`, but kinship crashes first → unreachable-with-dups → scope **PROVEN geneDrop-only** (contrast NEW-53/Learning #12, where the grep WIDENED scope to 3 sites; here it NARROWED to 1). Trace ALL upstream guards before declaring an item reachable. **(c) When the baseline is already-a-crash (Learning #8b), an in-repo SIBLING often supplies the contract + message precedent, and an invariant enforced by ≥2 siblings settles the DOMAIN without a sweep.** `kinship()` and `removeDuplicates()` already reject duplicate ids; `geneDrop`'s new guard mirrors `kinship`'s message (`"animal IDs must be unique"`). Because the unique-id domain was unambiguous (unlike NEW-45's sweep), grep+probe settled mechanism+reachability+domain → honest **no workflow** (5th consecutive). | Session 14 (NEW-46) | When fixing any audit "duplicate/corruption" item: (1) reproduce the ACTUAL terminal behavior, probing PAST any precursor warning (a both-handlers `tryCatch` hides the error after a warning) — R rejects duplicate data.frame row.names with a hard error, so `rownames(x)<-id` idioms CRASH, they don't mangle; (2) trace ALL upstream guards — reachability can be doubly-masked, and the `rownames<-`/dup grep inventory may NARROW scope by proving siblings unreachable; (3) when an in-repo sibling already enforces the same invariant, mirror its `stop()`/message and skip the domain sweep. |
| 15 | THREE lessons from NEW-16 (`summarizeKinshipValues` `secondQuartile` = `fivenum()[1]`/min instead of `[2]`/lower hinge; + O(n²) `rbind`-in-loop). **(a) When the audit's mechanism AND prescribed fix are BOTH correct (the NEW-45 case, 2nd time), the load-bearing risk shifts to the EXISTING TEST — a pre-existing test can silently PASS on the buggy output, so a naive RED test that re-asserts the existing fixture WON'T fail.** NEW-16: the existing `test_summarizeKinshipValues.R:75` asserts `secondQuartile[10]==0` and **still passes after the fix** because that row's lower hinge coincidentally equals its min (both 0) — the NEW-45 `C.003` "test passing on corrupted output" pattern. The discriminating RED test MUST use an input where buggy and correct DIVERGE: synthetic `numbers = 1:5` → `fivenum = 1,2,3,4,5`, so lower hinge `2` ≠ min `1`. Probe the REAL data first to (i) confirm reachability (here 5/153 rows change) and (ii) learn whether any existing assertion changes — BEFORE designing RED. **(b) TOOLING TRAP (recurs every R session): `testthat::test_dir()` run as a bare `Rscript -e '…'` WITHOUT `pkgload::load_all(".")` executes against the STALE INSTALLED namespace, not the working-tree source.** NEW-16's first run reported a PHANTOM **46 failed / 68 error** across ~40 files I never touched (every prior session's fix — geneDrop/calcFE/makeSimPed/hasInvalidIdChar/cumulateSimKinships — missing from the installed build) AND **inverted my own change** (the fixed file's new test ran against old installed code → "failed"). The TELL: dozens of offenders in files you never touched = an environment/loading problem, not a regression. The repo's documented "Clean regression read" command (CLAUDE.md "Build / Test / Verify") OMITS `load_all` — it is valid only in an already-loaded session or with an in-sync install. ALWAYS prepend `suppressMessages(pkgload::load_all("."))` for the authoritative read (corrected: 0/0/1977). **(c) Prove a behavior-preserving REFACTOR with `identical()`, not just "the suite still passes."** The `rbind`-in-loop → preallocated list + single `do.call(rbind, …)` was verified byte-identical against a pre-refactor reference captured on the seeded pipeline, the synthetic input, AND the all-skipped edge; preserve the empty-input contract (`do.call(rbind, list_of_all_NULL)` → `NULL`, so guard back to `data.frame()`). Mechanism+reachability grep/probe-settled → honest **no workflow** (6th consecutive). | Session 15 (NEW-16) | When fixing an audit item whose mechanism AND fix both check out (the NEW-45/NEW-16 "audit correct" branch): (1) assume a pre-existing test may be passing on the buggy output — design RED on an input where buggy≠correct, and probe real data for reachability + existing-assertion impact first; (2) ALWAYS run `test_dir` under `pkgload::load_all(".")` — a flood of failures in untouched files means the installed namespace is stale, not a regression; (3) prove any "no behavior change" refactor with `identical()` vs a captured reference, and preserve the empty-input contract. |
| 16 | FOUR lessons from PED-1/NEW-17 (extract `getFounders(ped)`/`isFounder(ped)`; de-duplicate the founder predicate `is.na(sire) & is.na(dam)` at **12 inline sites across 9 files** — the campaign's first pure-refactor deliverable, not a bug fix). **(a) For a fix-all-instances refactor, inventory by the PREDICATE/idiom, NOT the concept's NAME — variable naming hides sites.** `grep founder` found 10 sites; the broad `is.na(...sire/dam...)` sweep caught 2 more (`modSummaryStats.R:606/616`, named `males`/`females`, not `founders`). Separate the true idiom (BOTH parents NA) from false positives: roxygen `#'` examples, partial-parentage `xor(is.na(sire),is.na(dam))` / OR `is.na(sire)|is.na(dam)` / exactly-one-parent, `hasBothParents`, and unrelated `is.na()` (births/sex). The bare-vector site (`findPedigreeNumber.R:35`, `id[is.na(sire)&is.na(dam)]`, no `ped` object) doesn't fit a `getFounders(ped)` contract — leave it as a documented intentional exclusion; don't force a dual signature unless the author opts in. **(b) An extract-helper refactor is behavior-preserving BY CONSTRUCTION** (each site evaluates to the identical expression; R's three-valued `&` is associative/commutative even with NA, so `sex & isFounder(ped)` ≡ `sex & is.na(sire) & is.na(dam)`, and `sum(isFounder(ped))` ≡ `sum(mask, na.rm=TRUE)` since `is.na()` never returns NA) **— but PROVE it empirically anyway** with `identical()` vs a pre-refactor reference, INCLUDING the full output of a STOCHASTIC consumer captured under `set.seed` (here seeded `reportGV()`), so the deterministic founder path is verified inside a random function (extends Learning #15c). **(c) Lint provenance for a NEW cross-file helper: `object_usage_linter` "no visible global function definition for 'X'" on calls to the new function is the STALE-INSTALLED-NAMESPACE transient (Learning #7) — but the Learning-#7 `/tmp`-copy method is INVALID for `object_usage_linter`** because linting a file OUTSIDE the package dir loses BOTH the `.lintr` config AND the package namespace (HEAD-in-`/tmp` = 103 default-linter lints vs 21 in-package — not comparable). Authoritative reads: REINSTALL (`R CMD INSTALL`) so the namespace resolves the new function, then re-lint (warnings vanish → net-zero); and/or compare HEAD~1-vs-HEAD lint COUNTS **in-place** (both Shiny modules = 60/17 at each → my edits added 0). **(d) Orchestration:** a fix-all-instances refactor IS the canonical sweep shape — a read-only multi-angle completeness-sweep workflow (4 finders re-deriving the inventory by predicate / keyword / idiom / Shiny-layer) is the RIGHT tool (vs the prior 6 single-function bug fixes where grep settled it solo); all 4 converged on the 1 intentional exclusion → 0 misses, independently confirming completeness. | Session 16 (PED-1) | When doing any extract-helper / fix-all-instances refactor: (1) inventory by the IDIOM not the name, filtering false positives; (2) prove behavior-preservation with `identical()` vs a reference incl. a seeded stochastic consumer; (3) diagnose new-helper `object_usage` lints as the stale-namespace transient — verify via REINSTALL + re-lint and HEAD~1-in-place counts, NOT the `/tmp` method; (4) run a multi-angle completeness-sweep workflow to confirm no site was missed. |
| 17 | FOUR lessons from NEW-13/NEW-23 (extract the founder-contribution algorithm shared verbatim by `calcFE`/`calcFG`/`calcFEFG` into one `@noRd` `calcFounderContributions()`; collapse the triplicated S7 partial-parentage guard — the campaign's 2nd pure refactor after PED-1). **(a) A "delegate to X" audit framing can be literally UNIMPLEMENTABLE — check the signature asymmetry BEFORE designing.** `BACKLOG`/audit said "calcFE/calcFG delegate to calcFEFG," but `calcFE(ped)` takes no `alleles` and is deliberately gene-drop-free, while `calcFEFG` needs `alleles` (→ `calcRetention`) for FG; literal delegation would force calcFE to compute an FG it can't feed. The cleaner realization — a shared helper all three call — is a SUPERSET of the audit's intent (guard collapsed) that keeps calcFE gene-drop-free. Surface the approach fork (shared-`@noRd`-helper vs make-`calcFEFG`-`alleles`-optional) as a pre-RED `AskUserQuestion` (author decision); the user picked the helper. **(b) A behavior-preserving refactor's hidden divergence risk is in the WIRING you DON'T relocate, not the body you do.** The extracted body is identical-by-construction, but calcFG/calcFEFG reassigned `ped <- toCharacter(ped)` BEFORE `calcRetention(ped, alleles)`; moving `toCharacter` into the helper would feed `calcRetention` the ORIGINAL (possibly factor) ped — a silent divergence on factor input (which the tests exercise). VERIFY the downstream's sensitivity (read `calcRetention`: it's `%in%`-coercion-robust to factors) but don't RELY on it — make equivalence hold BY CONSTRUCTION: the helper returns `list(p, ped)` so the wrapper feeds `calcRetention` the SAME toCharacter-coerced ped the original did. **(c) TDD RED for a pure refactor = test the NEW unit's existence (the PED-1 pattern), because the cross-function characterization already PASSES.** `calcFE(ped) ≡ calcFEFG(ped, alleles)$FE` is TRUE today (bodies already identical) → NOT a RED test; the genuine RED is the `@noRd` helper not existing (`could not find function`). Preserve each function's exact error message through the single guard by parameterizing a `caller` arg (one guard, byte-identical messages — strictly more conservative than the generic message the existing `regexp="partial parentage"` tests would have tolerated). Note `.caller` would trip `object_name_linter` (`.lintr` styles = snake/Camel/camel; cf. `kinship.R`'s `# nolint` for `father.id`) → use `caller`. **(d) Right-sized orchestration under ultracode: the deterministic `identical()` proof IS the gold standard; the adversarial workflow CONFIRMS, it doesn't discover.** Proved byte-`identical()` on FE/FG (char AND factor), the full `set.seed(42)` `reportGV()` object (the live calcFEFG caller), and all 3 guard messages; THEN a 3-agent adversarial-equivalence workflow (static body-diff vs the GREEN commit via `git show`, 20 empirical OLD-vs-NEW edge tests transcribing the originals, contract/guard/namespace) independently returned 0 divergences. Lint net-zero (the new-helper `object_usage` finding is the Learning #16c stale-namespace transient — vanishes under `load_all`/reinstall). | Session 17 (NEW-13/NEW-23) | When doing any "delegate/consolidate" refactor: (1) check signature asymmetry — a literal "delegate to X" may be unimplementable; surface the approach fork as a pre-RED author `AskUserQuestion`; (2) audit the WIRING you don't relocate (arg coercion/state set before a downstream call) and make equivalence hold BY CONSTRUCTION (return the coerced object), after verifying the downstream's sensitivity; (3) RED-test the new unit's existence (cross-function characterization already passes); parameterize a single guard's message by `caller` (lint-safe name); (4) prove `identical()` on representative inputs + the seeded live caller, then let an adversarial workflow CONFIRM rather than discover. |
| 18 | FOUR lessons from NEW-22/NEW-30 (annotate the Mendelian ½ factor; remove the dead `UID.founders` block in `calcFounderContributions.R` + add Mendelian-½ comments in `kinship.R` — the campaign's first cleanup whose AUDIT MOTIVATION was already dissolved by a PRIOR session). **(a) An audit item's MOTIVATION can be made moot by earlier work — recognize it and DECLINE the mechanical fix rather than perform it reflexively (a new branch of #8a/#9a/#10a: not the mechanism wrong, the *reason gone*).** NEW-22 flagged the Mendelian ½ "hardcoded in 5 places" as DUPLICATION; but S17's NEW-13/NEW-23 already collapsed the 3 `calc*` copies into one helper, so the remaining `/ 2L` sites are 4–5 *distinct* formulas (parental-contribution avg, parental-kinship avg, self-kinship `(1+f)/2`, founder self-kinship init) split across the GV compute and the kinship engine — NOT one shared knob. A named constant would FALSELY imply they are the same value and over-couple two independent modules; `/2` is self-documenting in genetics. The disciplined output was a documented author DECLINE (comment-only), surfaced as a pre-RED `AskUserQuestion`, not a reflexive extraction. **(b) An audit's "dead variable" can be a deliberate optimization — verify EACH claimed-dead var is actually unused before deleting (the #8a pattern, now for dead-code claims).** `founderMatrix <- NULL` is NOT dead: `founderMatrix` is USED one line up (`d <- rbind(founderMatrix, d)`), and the `<- NULL` is an intentional memory free (drops the founders×founders identity block before the long generation loop). Only the `## UID.founders` *commented* block was genuinely dead. Keep+annotate the optimization; delete only the truly-unused. **(c) Strict TDD for a comment-only + dead-code-removal cleanup has NO RED/GREEN — it is REFACTOR by nature (no new behavior, no new unit). Don't manufacture a synthetic RED ("assert the comment exists") — that tests implementation trivia, the opposite of TDD.** Declare REFACTOR, gate `PRE-RED→REFACTOR` via `AskUserQuestion` (skipping RED/GREEN is HONEST here — there is no new behavior to develop — NOT the prohibited FM #17 erosion), and prove behavior-preservation by `identical()`: the change is parser-inert. **(d) Right-sizing under ultracode: a comments+dead-code change is BOTH "trivial" AND "already verified" — both escape clauses of "lean toward workflows unless trivial or already verified" — so NO adversarial workflow (it would be ceremony, beyond even S17's relocate-executable-code case).** `identical()` on the full `set.seed(42)` `reportGV()` object (the live `calcFEFG`/`kinship` caller) is conclusive. COROLLARY (lint #7): confirm FIRST that neither edited file is line-number-excluded in `.lintr` (the #7 shift-trap bites only excluded files — here neither was), and phrase prose comments WITHOUT code-like tokens (no `fn(args)`/bare `(1+f)/2`) so `commented_code_linter` does not flag them. | Session 18 (NEW-22/NEW-30) | When an audit item is "now trivial" because a prior session changed the code: (1) re-derive whether its MOTIVATION still holds — if a prior refactor dissolved it (duplication already removed), DECLINE the mechanical fix as an author decision, don't perform it; (2) verify each "dead" var is truly unused (a `<- NULL` after a use is a memory free, not dead); (3) treat a comment/dead-code cleanup as REFACTOR-only with no RED/GREEN — don't fake a RED; (4) skip the adversarial workflow when the change is parser-inert and `identical()` on the seeded live caller already proves it. |

### Project-specific Failure Modes

(none — the base failure modes #1–25 in `SESSION_RUNNER.md` apply.)
