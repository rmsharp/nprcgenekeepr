% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calcGeneDiversity.R
\name{calcGeneDiversity}
\alias{calcGeneDiversity}
\title{Calculate gene diversity from founder genome equivalents}
\usage{
calcGeneDiversity(fg)
}
\arguments{
\item{fg}{Founder genome equivalents scalar, as returned by \code{calcFG()}
or the \code{$FG} element of \code{calcFEFG()}. \code{NA} yields \code{NA}.}
}
\value{
The gene diversity \code{GD = 1 - 1 / (2 * fg)}: a single number in
\code{[0, 1)}, or \code{NA} when \code{fg} is \code{NA}.
}
\description{
Part of the Genetic Value Analysis
}
\details{
Gene diversity is the expected heterozygosity retained relative to the
founding gene pool, \code{GD = 1 - 1 / (2 * FG)}, where \code{FG} is the
founder genome equivalents (see \code{\link{calcFG}}). It summarizes how
much of the founders' allelic diversity still survives: 0 means none is
retained, and it approaches (never reaches) 1 as \code{FG} grows.

\code{GD} is a diversity proportion, not a count of effective individuals,
and it is computed over the same analysis set as \code{FG}. \code{NA}
propagates: when \code{FG} is \code{NA} (the zero-retention degeneracy
\code{calcFG()} reports), \code{GD} is \code{NA}.
}
\examples{
calcGeneDiversity(20) # 0.975
calcGeneDiversity(52.75) # gene diversity at the qcPed FG
}
\references{
Gene diversity is derived here from the founder genome
equivalents (\code{\link{calcFG}}) of Lacy RC. 1989. Analysis of founder
representation in pedigrees: founder equivalents and founder genome
equivalents. Zoo Biol 8:111-123.
}
\seealso{
Other genetic value analysis:
\code{\link[=calcA]{calcA()}},
\code{\link[=calcFE]{calcFE()}},
\code{\link[=calcFEFG]{calcFEFG()}},
\code{\link[=calcFG]{calcFG()}},
\code{\link[=calcFGSE]{calcFGSE()}},
\code{\link[=calcGU]{calcGU()}},
\code{\link[=calcGUSE]{calcGUSE()}},
\code{\link[=calcNeSexRatio]{calcNeSexRatio()}},
\code{\link[=calcNeVariance]{calcNeVariance()}},
\code{\link[=calcRetention]{calcRetention()}}
}
\concept{genetic value analysis}
