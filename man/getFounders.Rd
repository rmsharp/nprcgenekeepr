% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getFounders.R
\name{getFounders}
\alias{getFounders}
\title{Gets the founder ids from a pedigree}
\usage{
getFounders(ped)
}
\arguments{
\item{ped}{a pedigree \code{data.frame} with (at least) the columns
\code{id}, \code{sire}, and \code{dam}.}
}
\value{
A vector of the \code{id} values of the founders, in pedigree
order. It has the same type as \code{ped$id} and is empty when there are
no founders.
}
\description{
Part of Pedigree Curation
}
\details{
A founder is an animal whose sire and dam are both unknown (\code{NA}).
Animals with exactly one known parent (partial parentage) are
\strong{not} founders.
}
\examples{
library(nprcgenekeepr)
ped <- data.frame(
  id = c("A", "B", "C", "D", "E", "F", "G"),
  sire = c(NA, NA, "A", "A", NA, "D", "D"),
  dam = c(NA, NA, "B", "B", NA, "E", "E"),
  stringsAsFactors = FALSE
)
getFounders(ped)
}
\seealso{
\code{\link{isFounder}} for the founder logical mask.
}
