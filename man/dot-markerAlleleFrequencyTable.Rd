% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markerAlleleFrequency.R
\name{.markerAlleleFrequencyTable}
\alias{.markerAlleleFrequencyTable}
\title{Compute per-allele population frequency at one locus from a genotype matrix}
\usage{
.markerAlleleFrequencyTable(genotypeMatrix, locus)
}
\arguments{
\item{genotypeMatrix}{a character matrix as returned by
\code{\link{buildMarkerGenotypeMatrix}}: rows are individual \code{id}s,
columns are loci, and each cell is that individual's two alleles at that
locus, sorted and joined by \code{"/"} (or \code{NA} if not genotyped at
that locus).}

\item{locus}{a single locus name (one of \code{colnames(genotypeMatrix)}).}
}
\value{
A named numeric vector, allele -> frequency, computed from the
non-\code{NA} cells at \code{locus} only. A locus monomorphic among
genotyped individuals returns a length-1 vector.
}
\description{
Internal helper for \code{\link{markerParentageLikelihood}} (issue #147
Slice 1, D9). Mirrors \code{\link{markerExpectedHeterozygosity}}'s own
inline "parse alleles out of the \code{lo/hi} genotype string, tabulate"
pattern, factored into a small, independent, non-exported helper rather
than modifying that (or any other) already-shipped statistical function's
internals as a side effect of this feature (see this slice's own design
document, D9, for why a fourth independent reimplementation was chosen
over a shared extraction touching three existing files).
}
\keyword{internal}
