% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isFounder.R
\name{isFounder}
\alias{isFounder}
\title{Identify the founders in a pedigree}
\usage{
isFounder(ped)
}
\arguments{
\item{ped}{a pedigree \code{data.frame} with (at least) the columns
\code{sire} and \code{dam}.}
}
\value{
A logical vector with one element per row of \code{ped} that is
\code{TRUE} for each animal whose sire and dam are both \code{NA}. The
result never contains \code{NA}.
}
\description{
Part of Pedigree Curation
}
\details{
A founder is an animal whose sire and dam are both unknown (\code{NA}).
Animals with exactly one known parent (partial parentage) are
\strong{not} founders.
}
\examples{
library(nprcgenekeepr)
ped <- data.frame(
  id = c("A", "B", "C", "D", "E", "F", "G"),
  sire = c(NA, NA, "A", "A", NA, "D", "D"),
  dam = c(NA, NA, "B", "B", NA, "E", "E"),
  stringsAsFactors = FALSE
)
isFounder(ped)
}
\seealso{
\code{\link{getFounders}} for the founder \code{id} values.
}
